# 評価ケース（ADK evalset JSON）を置く。1ファイル = *.evalset.json。
# 1ケース = 入力（観察メモ + DocumentSpec）と期待値（採点の根拠 / must_fix 条件）。
# 15–30ケースで十分（数より質）。実データ（PII）は入れない＝架空の子のみ（設計コンテキスト §12/§14）。
# 子どもは実在しない仮名の固定ロスター（tests/test_eval_cases.py の _FICTIONAL_ROSTER）で表す。
